<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin - Manage Products</title>
    <style>
        /* Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: url('/css/car.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #f5f5f5;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            min-height: 100vh;
        }

        h1 {
            color: #FF5733; /* Racing Red */
            margin-bottom: 30px;
            font-size: 40px;
            background-color: rgba(0, 0, 0, 0.7);
            padding: 10px 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px #000;
        }

        form {
            background-color: rgba(0, 0, 0, 0.8);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 0 15px #000;
            width: 100%;
            max-width: 500px;
            margin-bottom: 30px;
        }

        form input, form textarea, form button {
            width: 100%;
            padding: 15px;
            margin: 12px 0;
            border: none;
            border-radius: 10px;
            background-color: #2a2a2a;
            color: #fff;
            font-size: 16px;
        }

        form button {
            background-color: #F1C40F; /* Neon Yellow */
            cursor: pointer;
            font-weight: bold;
            text-transform: uppercase;
            transition: 0.3s;
            box-shadow: 0 0 10px #000;
        }

        form button:hover {
            background-color: #FF5733;
            color: #fff;
            box-shadow: 0 0 20px #FF5733;
        }

        ul {
            list-style: none;
            width: 100%;
            max-width: 600px;
            background-color: rgba(0, 0, 0, 0.8);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 0 15px #000;
        }

        ul li {
            padding: 12px 0;
            border-bottom: 1px solid #444;
            color: #F1C40F;
            font-size: 18px;
            font-weight: bold;
            transition: 0.3s;
        }

        ul li:last-child {
            border-bottom: none;
        }

        ul li:hover {
            color: #FF5733;
            cursor: pointer;
            text-shadow: 0 0 5px #FF5733;
        }

        .hide-admin {
            display: none;
        }
    </style>
</head>
<body>
    <h1>Admin - Manage Products</h1>
    <form method="POST">
        <input name="name" placeholder="Product Name" required>
        <input name="price" placeholder="Price (e.g. 49.99)" required>
        <textarea name="description" placeholder="Description"></textarea>
        <button type="submit">Add Product</button>
    </form>
    <ul>
        <% products.forEach(p => { %>
            <li><%= p.name %> - $<%= p.price %></li>
        <% }) %>
    </ul>
</body>
</html>
